[mypy]
python_version = 3.11

# suppress missing‐stubs errors for all external libraries
ignore_missing_imports = True
follow_imports = silent

# don’t type-check tests (we’ll leave those for later)
exclude = ^tests/
